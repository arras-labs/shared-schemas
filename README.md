# Shared Schemas

## Overview
Shared contracts ABIs, data schemas, and TypeScript types used across services and clients.

## Scope
- Publish reusable types and schemas
- Provide ABIs generated by `contracts`
- Versioned releases for consumption

## Tech stack
TBD (e.g., TypeScript, JSON Schema, Protobuf)

## Usage
Consumers import the published package or artefacts from releases.

## Local development
- Install dependencies: `npm ci`
- Build: `npm run build`

## Testing
Add unit tests for schema validation and codecs.

## CI
Builds and tests package on PR and push. Release automation can be added later.
